// Import typeDefs and resolvers from their respective files
const typeDefs = require('./typeDefs');
const resolvers = require('./resolvers');

// Export them together
module.exports = {
  typeDefs,
  resolvers,
};
